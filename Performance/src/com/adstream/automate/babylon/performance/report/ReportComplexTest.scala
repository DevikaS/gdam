package com.adstream.automate.babylon
package performance.report

case class ReportComplexTest(
                              requestsPerMinute: Int,
                              responseTime: Double,
                              hitsCount: Int,
                              errorsCount: Int
                              )
